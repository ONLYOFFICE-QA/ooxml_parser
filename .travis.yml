language: ruby

rvm:
  - ruby-head
  - 2.7
  - 2.6
  - 2.5
  - 2.4
matrix:
  allow_failures:
  - rvm: ruby-head
install:
  - bundle install --without development
addons:
  apt:
    packages:
      - libmagic-dev
script:
  - bundle exec parallel_rspec spec
